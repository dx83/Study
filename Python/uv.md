# uv

```powershell
uv --version            # 버전 확인

uv init                 # .toml 프로젝트 관리 파일 생성
uv init --bare          # 샘플 파일을 만들지 않고 최소한으로 .toml 생성

uv add 패키지명          # 의존성 추가 명령어
uv remove 패키지명       # 패키지 제거
```

```powershell
uv run python 파일명.py  # 파이썬 실행ㅔ


```

