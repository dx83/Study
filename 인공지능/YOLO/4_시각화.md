### 시각화
- 분석한 이미지에 상자와 이름표를 그려줌
```python
rendered_img = result[0].plot()

# yolo라는 이름의 팝업창을 띄워 보여줌
cv2.imshow("yolo", rendered_img)
# 아무 키나 누를 때까지 창을 닫지 않고 무한히 대기
cv2.waitKey(0)
# 키보드를 눌러 대기가 끝나면, 열려 있던 OpenCV의 모든 팝업 창을 깔끔하게 닫고 메모리를 정리
cv2.destroyAllWindows()
```

<br>
