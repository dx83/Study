### 라이브러리 설치
```python
pip install ultralytics
```
<br>

### 사전 코드
```python
import os
os.environ["KMP_DUPLICATE_LIB_OK"] = "True"
```
- 연산라이브러리가 중복 로드되어 충돌하더라도 에러로 멈추지 말고 계속 실행

<br>

### 라이브러리 임포트
```python
from ultralytics import YOLO
model = YOLO('./pkl/yolov8s.pt')
```
<br>

### 객체 인식 가능한 목록 보기
```python
model.names
```
<br>
